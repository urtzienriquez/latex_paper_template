\documentclass[12pt]{article}

% page layout
\usepackage[margin=1in]{geometry}

% graphics and colors
\usepackage[dvipsnames]{xcolor}
\usepackage{graphicx}
\usepackage{float}

% math and tables
\usepackage{amsmath}
\usepackage{booktabs}

% bibliography
\usepackage[style=apa, backend=biber]{biblatex}

% authors and affiliations
\usepackage{authblk}
\renewcommand*{\Affilfont}{\footnotesize\normalfont}

% listings and text code formatting packages
\usepackage{listings}

% hyperlinks
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=Blue,
  filecolor=Maroon,
  citecolor=Blue,
  urlcolor=Blue
}

% custom commands for title page
\input{01_titlepage}
\date{\vspace{-5ex}}

\makeatletter
% fix authblk spacing
\renewcommand\AB@affilsepx{\protect\\\vspace{\baselineskip}\protect\Affilfont}

% store title and author for appendix
\let\papertitle\@title
\let\paperauthors\@author

% appendix title block
\newcommand{\appendixtitleblock}[1]{
  \begin{center}
    {\Large\textbf{Appendix #1} \textit{for:} \papertitle}
    \vskip 1em
      {\large\paperauthors}
  \end{center}
  \vskip 1.5em
}

% image width constraint
\def\maxwidth{%
  \ifdim\Gin@nat@width>\textwidth
    \textwidth
  \else
    \Gin@nat@width
  \fi
}
\makeatother



% listings configurations for inline \R
\lstset{
  language=R,
  basicstyle=\small\ttfamily\color{black},
  keywordstyle=\color[rgb]{0.125,0.290,0.529}\bfseries, % Matches \hlkwd
  stringstyle=\color[rgb]{0.306,0.604,0.024},           % Matches \hlstr
  commentstyle=\color[rgb]{0.561,0.349,0.008}\itshape,  % Matches \hlcom
  showstringspaces=false,
  breaklines=true
}

% inline R macro with adjusted padding box
\newcommand{\R}[1]{%
  \begingroup
  \setlength{\fboxsep}{1.5pt}
  \colorbox{shadecolor}{\lstinline[language=R]!#1!}
  \endgroup
}

% custom macro to setup the appendices
\newcommand{\appendixsetup}[1]{
  \newpage
  \newrefsection
  \setcounter{section}{0}
  \renewcommand{\theHsection}{app#1.\arabic{section}}
  \renewcommand{\thefigure}{S#1.\arabic{figure}}
  \renewcommand{\theHfigure}{S#1.\arabic{figure}}
  \setcounter{figure}{0}
  \renewcommand{\thetable}{S#1.\arabic{table}}
  \renewcommand{\theHtable}{S#1.\arabic{table}}
  \setcounter{table}{0}
}
